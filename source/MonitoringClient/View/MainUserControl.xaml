<UserControl
  x:Class="MonitoringClient.View.MainUserControl"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:local="clr-namespace:MonitoringClient.View"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  d:DesignHeight="450"
  d:DesignWidth="800"
  mc:Ignorable="d"
 >

  <Grid>
    <local:MonitoringView1 x:Name="LoggerView" Visibility="{Binding MonitoringVisibility, Mode=TwoWay}" />
    <local:AddLogEntryView1 x:Name="AddLogView" Visibility="{Binding AddLogEntryVisibility, Mode=TwoWay}" />

    <!--<local:AddLogEntryView1 x:Name="AddLogEntryView" Visibility="{Binding Path=AddLogEntryView}"></local:AddLogEntryView1>-->
  </Grid>
</UserControl>
